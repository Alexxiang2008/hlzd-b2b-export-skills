# Office PDF Toolkit 使用手册

> **让 Office 文档转换与 PDF 保护，从此变得简单**

| 项目 | 内容 |
|---|---|
| **产品名称** | Office PDF Toolkit |
| **版本** | v1.0 |
| **发布日期** | 2026-07-01 |
| **适用平台** | Windows / Linux / macOS |
| **适用对象** | 需要批量处理 Office 文档的销售、运营、技术支持人员 |

---

## 📖 关于本手册

本手册帮助您快速上手 **Office PDF Toolkit**——一款将 Office 文档（PPT/Word/Excel）转换为 PDF 并可选择加密保护的命令行工具集。

**适用场景**：
- 销售：将商务 PPT 转为 PDF 分发给客户
- 法务：对合同 PDF 加权限保护（防止外泄）
- 运营：批量处理产品手册、报价单
- 运维：服务器上自动化转换任务

**预计阅读时间**：5 分钟上手，15 分钟完全掌握。

---

## ✨ 产品能做什么

### 核心能力

| 能力 | 说明 |
|---|---|
| **格式转换** | PPT / Word / Excel → PDF |
| **PDF 加密** | 禁止打印、复制、修改 |
| **批量处理** | 支持多个文件并行处理 |
| **跨平台** | Windows / Linux / macOS 统一体验 |
| **一键流水线** | "转换 + 加密"一条命令完成 |

### 与传统方式对比

| 传统方式 | 本工具集 |
|---|---|
| 打开 Office → 文件 → 另存为 → PDF | 一行命令搞定 |
| 用 Adobe Acrobat 加密（需付费） | 命令行自动完成 |
| 多个文件需重复操作 | 支持批量自动化 |
| 不同平台操作不同 | 跨平台一致体验 |

---

## 🚀 快速开始（30 秒上手）

### 第 1 步：选择您的平台

#### Windows 用户

```bash
# 安装 Python 依赖
pip install pypdf pywin32
```

> 💡 **Microsoft Office** 是 Windows 上最佳转换引擎。如未安装，本工具会自动使用 LibreOffice。

#### Linux 用户（Ubuntu / Debian）

```bash
# 安装 LibreOffice 和中文字体
sudo apt install -y libreoffice fonts-noto-cjk

# 安装 Python 依赖
pip install pypdf
```

#### macOS 用户

```bash
# 安装 LibreOffice
brew install --cask libreoffice

# 安装 Python 依赖
pip install pypdf
```

### 第 2 步：解压工具集

```bash
unzip office-pdf-toolkit.zip -d /opt/
cd /opt/office-pdf-toolkit
```

### 第 3 步：运行第一个转换

```bash
# 把 report.pptx 转成 PDF
python scripts/pipeline.py report.pptx
```

✅ **完成！** 同目录下会出现 `report.pdf`。

### 进阶：转换 + 加密

```bash
# 转换并加密（自动生成保护密码）
python scripts/pipeline.py report.pptx --encrypt
```

✅ **完成！** 同目录下会同时出现 `report.pdf` 和 `report-受保护.pdf`。

---

## 📋 典型使用场景

### 场景 1：销售发客户资料

**需求**：把商务 PPT 转成 PDF，禁止客户修改或复制内容。

```bash
python scripts/pipeline.py 海联智达-商务版-v3.6.pptx --encrypt
```

**输出**：
- `海联智达-商务版-v3.6.pdf`（原始）
- `海联智达-商务版-v3.6-受保护.pdf`（加密，可发送客户）

### 场景 2：批量处理整个目录

**需求**：把 `/data/reports/` 下所有 PPT 都转 PDF。

```bash
for f in /data/reports/*.pptx; do
  python scripts/pipeline.py "$f"
done
```

### 场景 3：合同文档保护

**需求**：对已有合同 PDF 加权限保护。

```bash
python scripts/pdf_encrypt.py 合同文件.pdf
```

### 场景 4：服务器定时任务

**需求**：每天凌晨自动生成加密 PDF。

```bash
# 编辑 crontab
crontab -e

# 每天凌晨 2 点执行
0 2 * * * cd /opt/office-pdf-toolkit && python scripts/pipeline.py /data/daily.pptx --encrypt
```

### 场景 5：自定义输出位置

```bash
python scripts/pipeline.py report.pptx -o /path/to/output.pdf
```

---

## ⚙️ 命令参考

### 基础语法

```bash
python scripts/pipeline.py <输入文件> [选项]
```

### 完整参数

| 参数 | 简写 | 说明 | 示例 |
|---|---|---|---|
| `input` | - | 输入文件路径（必填） | `report.pptx` |
| `--output` | `-o` | 输出 PDF 路径 | `-o output.pdf` |
| `--encrypt` | - | 转换后立即加密 | `--encrypt` |
| `--user-pwd` | - | 设置用户密码（打开需输入） | `--user-pwd 123456` |
| `--owner-pwd` | - | 设置所有者密码（解除限制） | `--owner-pwd MyPass` |
| `--prefer` | - | 强制转换方式 | `--prefer libreoffice` |

### 使用示例

```bash
# 示例 1：最常用 - 转换并加密
python scripts/pipeline.py report.pptx --encrypt

# 示例 2：自定义输出
python scripts/pipeline.py report.pptx -o ~/Desktop/output.pdf

# 示例 3：用户密码保护
python scripts/pipeline.py report.pptx --encrypt --user-pwd abcd1234

# 示例 4：仅加密（已有 PDF）
python scripts/pdf_encrypt.py report.pdf
```

---

## 🔐 权限保护说明

### 默认保护模式

启用 `--encrypt` 后，PDF 默认禁止以下操作：

| 操作 | 状态 | 说明 |
|---|---|---|
| 打开阅读 | ✅ 允许 | 任何人都可打开 |
| 打印 | ❌ 禁止 | 防止纸质外泄 |
| 复制文本 | ❌ 禁止 | 防止内容被复制 |
| 复制图像 | ❌ 禁止 | 防止图片被提取 |
| 修改内容 | ❌ 禁止 | 防止篡改 |
| 编辑表单 | ❌ 禁止 | 防止表单被改写 |

### 密码说明

工具集生成 2 种密码：

| 密码类型 | 用途 | 是否必需 |
|---|---|---|
| **用户密码** | 打开 PDF 时需输入 | 可选（默认空） |
| **所有者密码** | 用于解除权限限制 | 自动生成 |

**示例输出**：
```
[OK] Encrypted: report-受保护.pdf
     Owner password: HLZD2026@ABCD1234
```

> ⚠️ **重要提示**：所有者密码**遗失无法恢复**！请妥善保存到密码管理器（如 1Password、Bitwarden）。

---

## 🌍 平台支持

| 平台 | 转换引擎 | 加密 | 推荐指数 |
|---|---|---|---|
| **Windows + Office** | PowerPoint COM | pypdf | ⭐⭐⭐⭐⭐ |
| **Windows + 无 Office** | LibreOffice | pypdf | ⭐⭐⭐⭐ |
| **Linux 服务器** | LibreOffice headless | pypdf | ⭐⭐⭐⭐ |
| **macOS** | LibreOffice | pypdf | ⭐⭐⭐⭐ |

**说明**：
- Windows + MS Office 保真度最高（视觉 1:1）
- 其他平台使用 LibreOffice（保真度也很好）
- 脚本自动检测最佳引擎

---

## ❓ 常见问题

### Q1: 转换很慢怎么办？

**A**: 首次启动 LibreOffice 需要 2-5 秒，属正常。优化建议：
- 启用服务模式（见下方"高级用法"）
- 避免大文件（>100MB）单次处理
- 使用本地 SSD 磁盘

### Q2: Linux 下中文显示为方块？

**A**: 安装中文字体包：
```bash
sudo apt install fonts-noto-cjk fonts-wqy-zenhei
fc-cache -fv
```

### Q3: 加密后忘记所有者密码？

**A**: **无法恢复**。请务必：
- 记录到密码管理器
- 或在合同中注明密码

### Q4: 多个文件可以并行处理吗？

**A**: 可以。用 `parallel` 命令：
```bash
ls *.pptx | parallel python scripts/pipeline.py {} --encrypt
```

### Q5: 支持哪些文件格式？

**A**:
- 输入：`.pptx` `.ppt` `.docx` `.doc` `.xlsx` `.xls`
- 输出：`.pdf`

### Q6: 能转换带密码的 PDF 吗？

**A**: 不能。工具集只能转换 Office 文件 + 加密普通 PDF。已有加密的 PDF 需要先用其他工具解密。

### Q7: 转换后的 PDF 与原文件视觉一致吗？

**A**: 99% 一致。极少数复杂元素（特殊字体、复杂动画）可能有微小差异。

### Q8: 命令在 Windows PowerShell 里能用吗？

**A**: 能。本工具集跨平台，PowerShell / CMD / Bash / Zsh 均可。

### Q9: 怎么集成到 Web 系统？

**A**: 用 Python API。详见 `references/usage.md`。

### Q10: 有 Docker 镜像吗？

**A**: 有 Dockerfile。详见 `references/linux_setup.md` § 4.3。

---

## 🔧 高级用法

### 1. 自定义权限

```python
from pdf_encrypt import add_custom_permissions_encryption

add_custom_permissions_encryption(
    input_pdf='report.pdf',
    output_pdf='report-restricted.pdf',
    owner_password='MyPass',
    allow_print=True,       # 允许打印
    allow_copy=False,        # 禁止复制
    allow_modify=False,      # 禁止修改
)
```

### 2. Web API 集成

```python
from flask import Flask, request, send_file
from pipeline import pipeline
import tempfile, os

app = Flask(__name__)

@app.route('/convert', methods=['POST'])
def convert_endpoint():
    f = request.files['file']
    encrypt = request.form.get('encrypt') == 'true'
    with tempfile.TemporaryDirectory() as tmp:
        src = os.path.join(tmp, f.filename)
        f.save(src)
        result = pipeline(src, encrypt=encrypt, kill_processes=False)
        response_path = result['protected'] if encrypt else result['pdf']
        return send_file(response_path, as_attachment=True)
```

### 3. Docker 部署

```dockerfile
FROM python:3.11-slim
RUN apt-get update && apt-get install -y --no-install-recommends \
    libreoffice fonts-noto-cjk fonts-wqy-zenhei
RUN pip install --no-cache-dir pypdf
COPY . /opt/office-pdf-toolkit
WORKDIR /opt/office-pdf-toolkit
ENTRYPOINT ["python", "scripts/pipeline.py"]
```

### 4. 性能优化（LibreOffice 服务模式）

```bash
# 启动服务（避免每次启动 2-5 秒）
soffice --headless --accept="socket,host=localhost,port=2002;urp;" --norestore --nologo &

# 用 unoconv 转换
unoconv -f pdf -o output.pdf input.pptx
```

---

## 🛠️ 故障排查

| 错误 | 原因 | 解决 |
|---|---|---|
| `LibreOffice not found` | 未安装 LibreOffice | `apt install libreoffice` |
| 中文显示方块 | 缺中文字体 | `apt install fonts-noto-cjk` |
| `soffice already running` | 锁文件冲突 | `pkill -9 -f soffice` |
| `Permission denied` | 文件被占用 | 关闭 Office 后重试 |
| Windows COM 错误 | Office 进程残留 | 关闭 PowerPoint 重试 |
| 转换 >30 秒 | 冷启动 | 预热或用服务模式 |

详细排查指南见 `references/troubleshooting.md`。

---

## 📊 性能参考

| 平台 | 文件 | 转换时间 | 加密时间 |
|---|---|---|---|
| Windows + Office | 10MB PPT | 3-5 秒 | < 1 秒 |
| Linux + LibreOffice | 10MB PPT | 5-10 秒 | < 1 秒 |
| Linux + 服务模式 | 10MB PPT | 1-3 秒 | < 1 秒 |

---

## 📦 完整包内容

```
office-pdf-toolkit/
├── README.md                  本使用手册
├── SKILL.md                   Claude Code 元数据
├── scripts/                   核心脚本
│   ├── platform_detect.py
│   ├── office_to_pdf.py
│   ├── pdf_encrypt.py
│   └── pipeline.py
└── references/                进阶文档
    ├── usage.md
    ├── linux_setup.md
    └── troubleshooting.md
```

---

## 🆘 获取帮助

| 渠道 | 说明 |
|---|---|
| **项目文档** | 同包 `references/` 目录 |
| **故障排查** | `references/troubleshooting.md` |
| **技术支持** | business@hailian-zhida.com |
| **官网** | https://hailian-zhida.com |

---

## 📜 版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-07-01 | 首发：跨平台 Office 转 PDF + 加密 |

---

## 📄 许可证

MIT License - 自由使用、修改、分发

---

> **海联智达** · 让中国工厂做全世界的生意
>
> © 2026 Hailian Zhida. All rights reserved.
