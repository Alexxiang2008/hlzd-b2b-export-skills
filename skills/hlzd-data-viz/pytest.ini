[pytest]
# 测试发现
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 输出
console_output_style = progress
addopts = -v --tb=short --strict-markers

# 标记
markers =
    slow: 慢速测试（如实际 API 调用）
    integration: 集成测试（需外部资源）

# 路径
pythonpath = scripts
